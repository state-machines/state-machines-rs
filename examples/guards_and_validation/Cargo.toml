[package]
name = "guards_and_validation"
version = "0.1.4"
edition = "2024"
publish = false

[dependencies]
state-machines = { path = "../../state-machines" }

[features]
default = []
inspect = ["state-machines/inspect"]
