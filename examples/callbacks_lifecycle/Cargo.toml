[package]
name = "callbacks_lifecycle"
version = "0.1.3"
edition = "2024"
publish = false

[dependencies]
state-machines = { path = "../../state-machines" }

[features]
default = []
inspect = ["state-machines/inspect"]
