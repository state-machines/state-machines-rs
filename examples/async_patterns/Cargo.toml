[package]
name = "async_patterns"
version = "0.1.5"
edition = "2024"
publish = false

[dependencies]
state-machines = { path = "../../state-machines", features = ["async"] }
pollster = "0.4"

[features]
default = []
inspect = ["state-machines/inspect"]
