[package]
name = "no_std_flight"
version = "0.0.4"
edition = "2021"
publish = false

[dependencies]
state-machines = { path = "../../state-machines", default-features = false }

[features]
default = []
inspect = ["state-machines/inspect"]
